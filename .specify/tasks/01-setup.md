# Task 01: Project Setup

## Description

Create project scaffolding: requirements.txt, directory structure, config template.

## Acceptance Criteria

- [x] `requirements.txt` with icalendar, requests, PyYAML
- [x] `src/` directory with `main.py`, `fetch.py`, `merge.py`, `__init__.py`
- [x] `config.example.yaml` documenting ical_sources (url + label) and gamesheets stub
- [x] `README.md` with setup, local run, and GitHub Actions + Pages instructions
- [x] `.gitignore` for config.yaml, feed.ics, .venv
